# Validation Report

## Status: PASS

## Completude (Discovery → SPEC)
Todos os itens do discovery estao cobertos na SPEC.

Verificacao realizada contra `PRD20260824_151851.md` e `stories-requisitos20260824_151851.md`:

- As 28 user stories (US-01 a US-28) estao mapeadas na tabela de rastreabilidade da SPEC (secao 1), cada uma com tela/funcao `render*`, contrato de mutacao e estrutura de dados correspondente.
- RF-01 a RF-48 tem cobertura: os quatro contratos de mutacao (`fn_gerar_ordens`, `fn_finalizar_operacao`, `fn_reservar_material`, `fn_publicar_ficha`) cobrem RF-01 a RF-14; os dominios C a J cobrem RF-15 a RF-47; RF-48 (D-04, sem US dedicada) esta explicitamente coberto na SPEC em 2.1.2 (`chave_dimensao`/`colisao_dimensao_aviso`), 3.2 (`importarVfb()` com aviso de colisao), 4.2 (componente do Dominio D) e 4.6 (estado "Aviso de colisao dimensional"), com nota de rastreabilidade dedicada na secao 1 — issue da rodada anterior resolvida.
- O gap 3 da Decisao F-6 do PRD (correcao de `renderQualidade()` populando `#qa-op` via `roteiroDe(ordem)` em vez de `ROT_BASE`) esta coberto na SPEC na linha de US-07 da tabela de rastreabilidade, em 3.2 (funcao reaproveitada `renderQualidade()`), 4.2 (Dominio C) e 4.3 ("Leitura de roteiro no posto de qualidade") — issue da rodada anterior resolvida.
- Os itens 4 e 6 da Decisao B-3 do PRD constam na secao 6 da SPEC ("Sequencia server-side para ID estavel de OS... Decisao B-3, item 4" e "Paginacao por `range` e exportacao de auditoria via PostgREST... Decisao B-3, item 6") — issue da rodada anterior resolvida.
- Os RNFs estao refletidos: RNF-01 (script de contraste nos 2 temas, 4.5), RNF-02 (dominios E/F), RNF-03 (`confirmarEfeito()` + regra de reversiveis sem confirmacao, 4.6), RNF-04 (`estorno_apontamento` com `janela_expira_em` = `criado_em + 10min`), RNF-05 (`origem.usuario` opaco, Decisao S-3), RNF-06 (`audit()`/`evento()` nos contratos de 3.2/3.3), RNF-07 (estados de rotulo explicito, 4.6), RNF-08 (exportacao apenas da lista filtrada, 4.3), RNF-09 (`fn_publicar_ficha` espelhando `fn_versao_ficha()`, 2.3), RNF-10 (regra `--accent`, 4.5).
- As decisoes do PRD estao refletidas: B-1/B-2/B-3 (secao 3), F-1 a F-6 (secao 4, incluindo os 3 gaps de F-6), S-1 a S-6 (secoes 2.2, 4.4 e 5), S-7/exportacao herdando filtro de tenant (4.3), D-04 (RF-48); as 4 decisoes adiadas (D-07, D-08, D-10, D-13) permanecem corretamente fora do escopo e listadas na secao 6, sem requisito de implementacao.
- Escopo negativo respeitado: nenhuma migration, Edge Function, RPC, policy RLS, tela nova, classe CSS nova ou dependencia de runtime aparece na SPEC como item de implementacao; tudo incide sobre `prototype/index.html`, conforme o PRD.
- Stack e plataforma conferem: HTML/CSS/JS single-file nesta fase, stack alvo (React 19 + Vite + Supabase, Vercel) documentada como referencia nao implementada; plataforma web desktop-first.
- Divergencia de perfis (8 no prototipo vs. 9 no discovery) registrada na SPEC via Decisao S-2, coerente com o PRD.
- Demais pendencias da Fase 4 do PRD (autorizacao server-side, perfil `admin`, buckets S7, idempotencia das mutacoes, migrations ausentes, trigger de `roteiros_base`, identificador opaco negociado com ViaSign, QR assinado/SHA-256, piso/teto de `PARAM_DEF.sessaoMin`, D-07/D-08/D-10) estao replicadas na secao 6 e em 4.4/5.1 da SPEC.

## Consistencia Backend ↔ Frontend
Backend e frontend estao consistentes.

- Nao ha endpoints HTTP nesta fase (Decisao B-1, coerente com o PRD); o contrato e de funcoes JS client-side. Cada funcao `fn_*` de 3.2 tem consumidor no frontend: `fn_gerar_ordens` no modal de preview do Planejamento (4.2, Dominio B), `fn_finalizar_operacao` em `renderOrdem`, `fn_reservar_material` na tela de Almoxarifado, `fn_publicar_ficha` no detalhe de ficha — todas precedidas de `confirmarEfeito()` conforme 4.6 (RNF-03).
- As leituras da camada de "API" (4.3) batem com os componentes que as exibem: fila de inspecao (`!o.qa`) → `#qa-fila-n` e stat do Inicio; retidas (`o.qa && !o.qa.aprovado`) → stat de Expedicao e `.ped-blk.retido`; `roteiroDe(ordem)` → pill "roteiro base herdado" e `#qa-op`; aderencia (`fim<=prazo`) → `renderDashboard()`; colisao dimensional → linha do importador; exportacao filtrada → `#aud-csv`/`#aud-pdf`.
- Auth: todas as telas do mapa 4.1 tem `perm` declarado, coerente com o roteador fail-closed de 3.3/4.4; sinais de handoff do Inicio condicionados a `perm()` por item (Decisao S-4 do PRD); fila do operador em modo leitura (`operador: r`) coerente com US-28/RF-47.
- Ausencia de SSE, paginacao de rede e error states HTTP e coerente: nao ha chamada de rede nesta fase (confirmado em 3.5 e 4.3), e os estados de UI relevantes (confirmacao, bloqueio, rotulo explicito, vazio, heranca, foco) estao definidos em 4.6.

## Consistencia Database ↔ Backend
Database e backend estao consistentes.

- Tabelas Supabase (2.1.1) sao referencia congelada, sem alteracao, coerente com a decisao "nenhuma migration/ALTER" do PRD; nenhum contrato da secao 3 grava nelas, e o congelamento e consistente entre 2.0, 3.0, 5.0 e as Decisoes B-1/S-1.
- Todos os campos novos de 2.1.2 tem consumidor: `roteiro_herdado` (pill de heranca, 4.3), `bloqueio_motivo` (fila do operador, US-28), `eh_lote` (avanco por pedido, US-27), `criada_em`/`liberada_em` (Recalcular/Liberar em `fn_reservar_material`), `estorno_apontamento` (`fn_finalizar_operacao`, janela de 10min de RF-11/RNF-04), `os_ids[]`/`grupo_chave` (etiqueta/QR, US-24), `rascunho`/`versao_publicada` (`fn_publicar_ficha`/`descartarFicha()`), `area_geometrica_m2`/`faturamento_criterio` (US-26), `VFB_PEL` tipo IV e `formato_variante: sem_quadro` (`mapearPlacaVfb()`), `colisao_dimensao_aviso` (`importarVfb()`, RF-48), `EXCECOES_RANGE_PAINEL` (Painel, RF-36).
- Os campos usados nos calculos existem nas estruturas: `fim`/`prazo` (aderencia), `qa.aprovado` (retidas), `roteiro[].status`/`baixado` (guard-rail de reapontamento), `un` (isolamento por tenant na auditoria/exportacao), `snap`/`cfg` (ordens afetadas por publicacao).
- O diagrama ER (2.5) reflete os mesmos relacionamentos das estruturas e contratos; a convencao append-only e respeitada nos dois lados (`estorno_apontamento` local espelha `estornos_apontamento`; `fn_publicar_ficha` espelha `fn_versao_ficha()`, nunca UPDATE de conteudo — RNF-09).
- Seed: unico dado de referencia novo (`VFB_PEL` tipo IV) declarado em 2.4 e consumido por `mapearPlacaVfb()`.

## Resumo
- Total de issues: 0
